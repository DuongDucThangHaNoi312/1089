{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
        <a href="{{ path('admin_app_jobannouncement_view_list', {
                'filter[jobAnnouncement][value]': object.id }) }}">
                {{  admin.getJobAnnouncementViewCount(object)  }}
        </a>
{% endblock %}